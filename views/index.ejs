<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title>filmnerds</title>
  </head>
  <body>
    <%- include("partials/header") %>
    <% if (locals.isAuth) { %>
    <p>
      Welcome, <b><%= locals.user.username %></b>! You are logged in
    </p>

    <% } %>
  </body>
</html>
